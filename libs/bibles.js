/* TODO:
* jsdoc
* constructor: Bibles(Folder)
* get all XML in Folder.
* call Bible(xml) on them, return into this.bibles[]
* create this.structure (bible structure (books, chapter-numbers, verse-count))
* create this.languages ( lang: [version1, version2, ...])
* methods:
* getStructure
* getBook
* getChapter
* getVerseCode(book, chapter, number)
* search(text) -> search in all bibles
* getLanguages
* getVersionsForLanguage(lang)
* getBible(lang, version)
*/
