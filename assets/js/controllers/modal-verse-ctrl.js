/* global angular */
;(function () {
  angular.module('myVerses')
    .controller('myVerses-Modal-Verse', ['myVersesUserProvider', 'myVersesBibleProvider', '$rootScope', function (myVersesUserProvider, myVersesBibleProvider, $rootScope) {
      // TODO: display form, fill fields, and write back to user-data
    }])
}())
