/* global angular */
;(function () {
  angular.module('myVerses')
    .controller('myVerses-Profile', ['myVersesUserProvider', function (myVersesUserProvider) {
      // TODO: load user by id, then fill data. allow writeback
    }])
}())
