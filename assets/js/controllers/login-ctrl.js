/* global angular */
;(function () {
  angular.module('myVerses')
    .controller('myVerses-Login', ['myVersesUserProvider', '$rootScope', function (myVersesUserProvider, $rootScope) {
      // TODO: check the login and may write data to rootScope. then redirect
    }])
}())
