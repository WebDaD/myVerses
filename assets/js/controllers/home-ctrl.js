/* global angular */
;(function () {
  angular.module('myVerses')
    .controller('myVerses-Home', ['$rootScope', 'myVersesUserProvider', '$scope', 'mySocket', function ($rootScope, myVersesUserProvider, $scope, mySocket) {
      // TODO: load cards for given user (rootScope)
    }])
}())
