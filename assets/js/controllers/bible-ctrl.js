/* global angular */
;(function () {
  angular.module('myVerses')
    .controller('myVerses-Bible', ['myVersesBibleProvider', '$rootScope', function (myVersesBibleProvider, $rootScope) {
      // TODO: display what is wanted (bible, book, chapter, verse) (lang and version from rootScope preselected)
    }])
}())
