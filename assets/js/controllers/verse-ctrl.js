/* global angular */
;(function () {
  angular.module('myVerses')
    .controller('myVerses-Verse', ['myVersesBibleProvider', function (myVersesBibleProvider) {
      // TODO: show a single verse, may be a users one, if there is an id
    }])
}())
